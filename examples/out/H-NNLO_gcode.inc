*--#[ GT1 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:55:47.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d6 (hb), p1 - v1 + v2, 5 -- 4
*   2: d7 (gl), v1 - v2,      6 -- 4
*   3: d5 (gl), -p2 + v2,     6 -- 3
*   4: d4 (gl), -v2,          5 -- 3
*   5: d2 (gl), -p1 + v1,     5 -- 1
*   6: d1 (gl), -v1,          2 -- 1
*   7: d3 (gl), p2 - v1,      6 -- 2
* (scheme w/o completion)
#-

#include `ROOT'/def/BT3.def

#procedure zGT1
* vanishing sub-topologies
if ((count(d6,1) >= 0)) 
  discard;
if ((count(d5,1) >= 0)) 
  discard;
if ((count(d1,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT1(MM)
* no partial fraction decomposition
#endprocedure

#procedure mGT1tBT3(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,6,7) to (1,2,3,4,5,6,7)
id (`marker') = BT3;
mu replace_(d6,d6, d7,d7, d5,d5, d4,d4, d2,d2, d1,d1, d3,d3);
mu replace_(v1,v1, v2,v2);
.sort
#call cBT3((`MM'))
.sort
#call gBT3((`MM'),(`M2ep'))
.sort
#call zBT3
.sort
#endprocedure

#procedure GT1
#message this is "GT1"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT1
#call pGT1(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT1tBT3(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT1\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT3'
#message symmetrize "BT3"...
#call yBT3
.sort
#endif
* load reduction tables
#ifdef `useBT3'
#message read table for "BT3"...
#include `ROOT'/tab/BT3.inx
.sort
#endif
#endprocedure

*--#] GT1 :



*--#[ GT10 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:55:49.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d7 (gh), p1 + p2 - v1 - v2, 5 -- 6
*   2: d4 (hb), -v2,               4 -- 3
*   3: d6 (gl), p1 - v2,           6 -- 4
*   4: d5 (gl), -p2 + v2,          5 -- 3
*   5: d3 (gh), p2 - v1,           6 -- 2
*   6: d2 (gh), p1 - v1,           1 -- 5
*   7: d1 (gh), -v1,               2 -- 1
* (scheme w/o completion)
#-

#include `ROOT'/def/BT1.def

#procedure zGT10
* vanishing sub-topologies
if ((count(d7,1) >= 0)) 
  discard;
if ((count(d4,1) >= 0)) 
  discard;
if ((count(d1,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT10(MM)
* no partial fraction decomposition
#endprocedure

#procedure mGT10tBT1(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,6,7) to (1,2,3,4,5,6,7)
id (`marker') = BT1;
mu replace_(d7,d7, d4,d4, d6,d6, d5,d5, d3,d3, d2,d2, d1,d1);
mu replace_(v1,v1, v2,v2);
.sort
#call cBT1((`MM'))
.sort
#call gBT1((`MM'),(`M2ep'))
.sort
#call zBT1
.sort
#endprocedure

#procedure GT10
#message this is "GT10"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT10
#call pGT10(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT10tBT1(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT10\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT1'
#message symmetrize "BT1"...
#call yBT1
.sort
#endif
* load reduction tables
#ifdef `useBT1'
#message read table for "BT1"...
#include `ROOT'/tab/BT1.inx
.sort
#endif
#endprocedure

*--#] GT10 :



*--#[ GT11 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:55:52.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d7 (hb), -p1 - p2 + v1 + v2, 6 -- 5
*   2: d1 (gl), -v1,                2 -- 1
*   3: d2 (gl), -p1 + v1,           5 -- 1
*   4: d3 (gl), p2 - v1,            6 -- 2
*   5: d4 (gl), -v2,                4 -- 3
*   6: d6 (gl), p1 - v2,            6 -- 4
*   7: d5 (gl), -p2 + v2,           5 -- 3
* (scheme w/o completion)
#-

#include `ROOT'/def/BT2.def

#procedure zGT11
* vanishing sub-topologies
if ((count(d7,1) >= 0)) 
  discard;
if ((count(d1,1) >= 0)) 
  discard;
if ((count(d4,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT11(MM)
* no partial fraction decomposition
#endprocedure

#procedure mGT11tBT2(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,6,7) to (1,2,3,4,5,6,7)
id (`marker') = BT2;
mu replace_(d7,d7, d1,d1, d2,d2, d3,d3, d4,d4, d6,d6, d5,d5);
mu replace_(v1,v1, v2,v2);
.sort
#call cBT2((`MM'))
.sort
#call gBT2((`MM'),(`M2ep'))
.sort
#call zBT2
.sort
#endprocedure

#procedure GT11
#message this is "GT11"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT11
#call pGT11(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT11tBT2(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT11\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT2'
#message symmetrize "BT2"...
#call yBT2
.sort
#endif
* load reduction tables
#ifdef `useBT2'
#message read table for "BT2"...
#include `ROOT'/tab/BT2.inx
.sort
#endif
#endprocedure

*--#] GT11 :



*--#[ GT12 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:55:55.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d7 (hb), -p2 - v1 - v2, 6 -- 5
*   2: d5 (gl), -v2,           5 -- 4
*   3: d6 (gl), p1 + v2,       6 -- 4
*   4: d3 (gl), -p2 - v1,      5 -- 2
*   5: d4 (gl), -p1 + p2 + v1, 6 -- 3
*   6: d2 (gl), -p1 + v1,      3 -- 1
*   7: d1 (gl), -v1,           2 -- 1
* (scheme w/o completion)
#-

#include `ROOT'/def/BT2.def
#include `ROOT'/def/BT5.def
#include `ROOT'/def/BT6.def

#procedure zGT12
* vanishing sub-topologies
if ((count(d7,1) >= 0)) 
  discard;
if ((count(d5,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT12(MM)
* partial fraction decomposition:
*   d1 - d2 - d3 + d4 - s
id d1 = d2 + d3 - d4 + (`MM');
id d2/d1 = (d1 - d3 + d4 - (`MM'))/d1;
id d4/(d1*d2) = -((d1 - d2 - d3 - (`MM'))/(d1*d2));
id d3/(d1*d2*d4) = (d1 - d2 + d4 - (`MM'))/(d1*d2*d4);
id 1/(d1*d2*d3*d4) = (d1 - d2 - d3 + d4)/(d1*d2*d3*d4*(`MM'));
* kinematic constraints
id p1.p1 = 0;
id p2.p2 = 0;
id p1.p2 = -(`MM')/2;
#endprocedure

#procedure mGT12tBT2(marker,MM,M2ep)
* subset 1
* map from (1,2,3,5,6,7) to (1,2,3,7,5,6)
if ((count(d3,1) == 0) && match(`marker'));
id (`marker') = BT2;
mu replace_(d7,d7, d5,d1, d6,d2, d4,d5, d2,d4, d1,d6);
mu replace_(v1,p1 - v2, v2,-v1);
endif;
.sort
#call cBT2((`MM'))
.sort
#call gBT2((`MM'),(`M2ep'))
.sort
#call zBT2
.sort
#endprocedure

#procedure mGT12tBT5(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,6,7) to (1,3,4,5,6,7)
if ((count(d4,1) == 0) && match(`marker'));
id (`marker') = BT5;
mu replace_(d7,d7, d5,d5, d6,d4, d3,d3, d2,d2, d1,d1);
mu replace_(v1,-v1, v2,-p1 + v2);
endif;
.sort
#call cBT5((`MM'))
.sort
#call gBT5((`MM'),(`M2ep'))
.sort
#call zBT5
.sort
#endprocedure

#procedure mGT12tBT6(marker,MM,M2ep)
* subset 1
* map from (1,3,2,4,5,6) to (1,3,4,5,6,7)
if ((count(d1,1) == 0) && match(`marker'));
id (`marker') = BT6;
mu replace_(d7,d6, d6,d5, d5,d4, d3,d2, d4,d1, d2,d3);
mu replace_(v1,p1 - p2 + v1, v2,-v2);
endif;
.sort
#call cBT6((`MM'))
.sort
#call gBT6((`MM'),(`M2ep'))
.sort
#call zBT6
.sort
#endprocedure

#procedure GT12
#message this is "GT12"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT12
#call pGT12(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT12tBT2(marker,s,[(mu^2/s)^ep])
.sort
#call mGT12tBT5(marker,s,[(mu^2/s)^ep])
.sort
#call mGT12tBT6(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT12\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT2'
#message symmetrize "BT2"...
#call yBT2
.sort
#endif
#ifdef `symBT5'
#message symmetrize "BT5"...
#call yBT5
.sort
#endif
#ifdef `symBT6'
#message symmetrize "BT6"...
#call yBT6
.sort
#endif
* load reduction tables
#ifdef `useBT2'
#message read table for "BT2"...
#include `ROOT'/tab/BT2.inx
.sort
#endif
#ifdef `useBT5'
#message read table for "BT5"...
#include `ROOT'/tab/BT5.inx
.sort
#endif
#ifdef `useBT6'
#message read table for "BT6"...
#include `ROOT'/tab/BT6.inx
.sort
#endif
#endprocedure

*--#] GT12 :



*--#[ GT13 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:56:03.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d7 (gl), -p2 - v1 - v2, 6 -- 5
*   2: d5 (hb), -v2,           5 -- 4
*   3: d6 (gl), p1 + v2,       6 -- 4
*   4: d3 (gl), -p2 - v1,      5 -- 2
*   5: d1 (gl), -v1,           2 -- 1
*   6: d4 (gl), -p1 + p2 + v1, 6 -- 3
*   7: d2 (gl), -p1 + v1,      3 -- 1
* (scheme w/o completion)
#-

#include `ROOT'/def/BT1.def
#include `ROOT'/def/BT5.def
#include `ROOT'/def/BT6.def

#procedure zGT13
* vanishing sub-topologies
if ((count(d7,1) >= 0)) 
  discard;
if ((count(d5,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT13(MM)
* partial fraction decomposition:
*   -d1 + d2 + d3 - d4 + s
id d2 = d1 - d3 + d4 - (`MM');
id d4/d2 = -((d1 - d2 - d3 - (`MM'))/d2);
id d1/(d2*d4) = (d2 + d3 - d4 + (`MM'))/(d2*d4);
id d3/(d1*d2*d4) = (d1 - d2 + d4 - (`MM'))/(d1*d2*d4);
id 1/(d1*d2*d3*d4) = (d1 - d2 - d3 + d4)/(d1*d2*d3*d4*(`MM'));
* kinematic constraints
id p1.p1 = 0;
id p2.p2 = 0;
id p1.p2 = -(`MM')/2;
#endprocedure

#procedure mGT13tBT1(marker,MM,M2ep)
* subset 1
* map from (1,2,3,5,6,7) to (1,2,3,6,5,7)
if ((count(d3,1) == 0) && match(`marker'));
id (`marker') = BT1;
mu replace_(d7,d7, d5,d4, d6,d6, d1,d2, d4,d3, d2,d1);
mu replace_(v1,p1 - v1, v2,-v2);
endif;
.sort
#call cBT1((`MM'))
.sort
#call gBT1((`MM'),(`M2ep'))
.sort
#call zBT1
.sort
#endprocedure

#procedure mGT13tBT5(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,6,7) to (3,1,2,5,7,6)
if ((count(d1,1) == 0) && match(`marker'));
id (`marker') = BT5;
mu replace_(d7,d5, d5,d7, d6,d6, d3,d3, d4,d1, d2,d2);
mu replace_(p1,p2, p2,p1);
mu replace_(v1,-p1 + p2 - v1, v2,p1 - p2 + v1 - v2);
endif;
.sort
#call cBT5((`MM'))
.sort
#call gBT5((`MM'),(`M2ep'))
.sort
#call zBT5
.sort
#endprocedure

#procedure mGT13tBT6(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,7) to (4,1,2,5,6,7)
if ((count(d4,1) == 0) && match(`marker'));
id (`marker') = BT6;
mu replace_(d7,d4, d5,d6, d6,d7, d3,d2, d1,d1, d2,d3);
mu replace_(p1,p2, p2,p1);
mu replace_(v1,v1, v2,-p1 - v1 + v2);
endif;
.sort
#call cBT6((`MM'))
.sort
#call gBT6((`MM'),(`M2ep'))
.sort
#call zBT6
.sort
#endprocedure

#procedure GT13
#message this is "GT13"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT13
#call pGT13(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT13tBT1(marker,s,[(mu^2/s)^ep])
.sort
#call mGT13tBT5(marker,s,[(mu^2/s)^ep])
.sort
#call mGT13tBT6(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT13\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT1'
#message symmetrize "BT1"...
#call yBT1
.sort
#endif
#ifdef `symBT5'
#message symmetrize "BT5"...
#call yBT5
.sort
#endif
#ifdef `symBT6'
#message symmetrize "BT6"...
#call yBT6
.sort
#endif
* load reduction tables
#ifdef `useBT1'
#message read table for "BT1"...
#include `ROOT'/tab/BT1.inx
.sort
#endif
#ifdef `useBT5'
#message read table for "BT5"...
#include `ROOT'/tab/BT5.inx
.sort
#endif
#ifdef `useBT6'
#message read table for "BT6"...
#include `ROOT'/tab/BT6.inx
.sort
#endif
#endprocedure

*--#] GT13 :



*--#[ GT14 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:56:10.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d7 (gl), -p2 - v1 - v2, 6 -- 5
*   2: d5 (gl), -v2,           5 -- 4
*   3: d6 (gl), p1 + v2,       6 -- 4
*   4: d3 (hb), -p2 - v1,      5 -- 2
*   5: d1 (gl), -v1,           2 -- 1
*   6: d4 (gl), -p1 + p2 + v1, 6 -- 3
*   7: d2 (gl), -p1 + v1,      3 -- 1
* (scheme w/o completion)
#-

#include `ROOT'/def/BT7.def

#procedure zGT14
* vanishing sub-topologies
if ((count(d7,1) >= 0)) 
  discard;
if ((count(d3,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0)) 
  discard;
if ((count(d5,1) >= 0) &&
    (count(d6,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT14(MM)
* partial fraction decomposition:
*   -d1 + d2 + d3 - d4 + s - s*x
id d2 = d1 - d3 + d4 - (`MM')*[1-x];
id d4/d2 = -((d1 - d2 - d3 - (`MM')*[1-x])/d2);
id d1/(d2*d4) = (d2 + d3 - d4 + (`MM')*[1-x])/(d2*d4);
id d3/(d1*d2*d4) = (d1 - d2 + d4 - (`MM')*[1-x])/(d1*d2*d4);
id 1/(d1*d2*d3*d4) = (d1 - d2 - d3 + d4)/(d1*d2*d3*d4*(`MM')*[1-x]);
* kinematic constraints
id p1.p1 = 0;
id p2.p2 = 0;
id p1.p2 = -(`MM')/2;
#endprocedure

#procedure mGT14tBT7(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,7) to (1,3,4,5,6,7)
if ((count(d4,1) == 0) && match(`marker'));
id (`marker') = BT7;
mu replace_(d7,d7, d5,d5, d6,d4, d3,d3, d1,d1, d2,d2);
mu replace_(v1,-v1, v2,-p1 + v2);
endif;
* subset 2
* map from (1,2,3,4,6,7) to (3,1,2,5,6,7)
if ((count(d1,1) == 0) && match(`marker'));
id (`marker') = BT7;
mu replace_(d7,d5, d5,d7, d6,d6, d3,d3, d4,d1, d2,d2);
mu replace_(p1,p2, p2,p1);
mu replace_(v1,-p1 + p2 - v1, v2,p1 - p2 + v1 - v2);
endif;
.sort
#call cBT7((`MM'))
.sort
#call gBT7((`MM'),(`M2ep'))
.sort
#call zBT7
.sort
#endprocedure

#procedure GT14
#message this is "GT14"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT14
#call pGT14(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT14tBT7(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT14\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT7'
#message symmetrize "BT7"...
#call yBT7
.sort
#endif
* load reduction tables
#ifdef `useBT7'
#message read table for "BT7"...
#include `ROOT'/tab/BT7.inx
.sort
#endif
#endprocedure

*--#] GT14 :



*--#[ GT15 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:56:15.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d7 (gh), -p2 - v1 - v2, 6 -- 5
*   2: d6 (gh), -p1 - v2,      4 -- 6
*   3: d5 (gh), -v2,           5 -- 4
*   4: d2 (hb), -p1 + v1,      3 -- 1
*   5: d1 (gl), -v1,           2 -- 1
*   6: d4 (gl), -p1 + p2 + v1, 6 -- 3
*   7: d3 (gl), -p2 - v1,      5 -- 2
* (scheme w/o completion)
#-

#include `ROOT'/def/BT1.def
#include `ROOT'/def/BT7.def

#procedure zGT15
* vanishing sub-topologies
if ((count(d7,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0)) 
  discard;
if ((count(d5,1) >= 0) &&
    (count(d6,1) >= 0)) 
  discard;
if ((count(d3,1) >= 0) &&
    (count(d5,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT15(MM)
* partial fraction decomposition:
*   -d1 + d2 + d3 - d4 + s - s*x
id d3 = d1 - d2 + d4 - (`MM')*[1-x];
id d4/d3 = -((d1 - d2 - d3 - (`MM')*[1-x])/d3);
id d1/(d3*d4) = (d2 + d3 - d4 + (`MM')*[1-x])/(d3*d4);
id d2/(d1*d3*d4) = (d1 - d3 + d4 - (`MM')*[1-x])/(d1*d3*d4);
id 1/(d1*d2*d3*d4) = (d1 - d2 - d3 + d4)/(d1*d2*d3*d4*(`MM')*[1-x]);
* kinematic constraints
id p1.p1 = 0;
id p2.p2 = 0;
id p1.p2 = -(`MM')/2;
#endprocedure

#procedure mGT15tBT1(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,6) to (1,5,7,2,4,3)
if ((count(d3,1) == 0) && match(`marker'));
id (`marker') = BT1;
mu replace_(d7,d7, d6,d3, d5,d1, d2,d4, d1,d5, d4,d6);
mu replace_(p1,p2, p2,p1);
mu replace_(v1,p2 - v2, v2,-v1);
endif;
.sort
#call cBT1((`MM'))
.sort
#call gBT1((`MM'),(`M2ep'))
.sort
#call zBT1
.sort
#endprocedure

#procedure mGT15tBT7(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,6,7) to (2,3,4,5,6,7)
if ((count(d1,1) == 0) && match(`marker'));
id (`marker') = BT7;
mu replace_(d7,d6, d6,d5, d5,d4, d2,d3, d4,d1, d3,d2);
mu replace_(v1,p1 - p2 + v1, v2,-v2);
endif;
* subset 2
* map from (1,2,3,4,5,7) to (4,1,2,5,6,7)
if ((count(d4,1) == 0) && match(`marker'));
id (`marker') = BT7;
mu replace_(d7,d4, d6,d7, d5,d6, d2,d3, d1,d1, d3,d2);
mu replace_(p1,p2, p2,p1);
mu replace_(v1,v1, v2,-p1 - v1 + v2);
endif;
.sort
#call cBT7((`MM'))
.sort
#call gBT7((`MM'),(`M2ep'))
.sort
#call zBT7
.sort
#endprocedure

#procedure GT15
#message this is "GT15"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT15
#call pGT15(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT15tBT1(marker,s,[(mu^2/s)^ep])
.sort
#call mGT15tBT7(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT15\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT1'
#message symmetrize "BT1"...
#call yBT1
.sort
#endif
#ifdef `symBT7'
#message symmetrize "BT7"...
#call yBT7
.sort
#endif
* load reduction tables
#ifdef `useBT1'
#message read table for "BT1"...
#include `ROOT'/tab/BT1.inx
.sort
#endif
#ifdef `useBT7'
#message read table for "BT7"...
#include `ROOT'/tab/BT7.inx
.sort
#endif
#endprocedure

*--#] GT15 :



*--#[ GT2 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:56:23.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d4 (gh), -v2,           5 -- 3
*   2: d5 (gh), p2 - v2,       3 -- 6
*   3: d7 (gh), v1 - v2,       6 -- 4
*   4: d6 (gh), -p1 + v1 - v2, 4 -- 5
*   5: d1 (hb), -v1,           2 -- 1
*   6: d2 (gl), -p1 + v1,      5 -- 1
*   7: d3 (gl), p2 - v1,       6 -- 2
* (scheme w/o completion)
#-

#include `ROOT'/def/BT4.def

#procedure zGT2
* vanishing sub-topologies
if ((count(d5,1) >= 0)) 
  discard;
if ((count(d6,1) >= 0)) 
  discard;
if ((count(d1,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT2(MM)
* no partial fraction decomposition
#endprocedure

#procedure mGT2tBT4(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,6,7) to (1,2,3,4,5,6,7)
id (`marker') = BT4;
mu replace_(d4,d4, d5,d5, d7,d7, d6,d6, d1,d1, d2,d2, d3,d3);
mu replace_(v1,v1, v2,v2);
.sort
#call cBT4((`MM'))
.sort
#call gBT4((`MM'),(`M2ep'))
.sort
#call zBT4
.sort
#endprocedure

#procedure GT2
#message this is "GT2"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT2
#call pGT2(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT2tBT4(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT2\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT4'
#message symmetrize "BT4"...
#call yBT4
.sort
#endif
* load reduction tables
#ifdef `useBT4'
#message read table for "BT4"...
#include `ROOT'/tab/BT4.inx
.sort
#endif
#endprocedure

*--#] GT2 :



*--#[ GT3 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:56:26.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d7 (hb), p1 - p2 + v1 - v2, 6 -- 4
*   2: d6 (gl), -p1 - v1 + v2,     5 -- 4
*   3: d5 (gl), -p1 + v2,          6 -- 3
*   4: d4 (gl), -v2,               5 -- 3
*   5: d3 (gl), p2 - v1,           6 -- 2
*   6: d2 (gl), p1 + v1,           5 -- 1
*   7: d1 (gl), -v1,               2 -- 1
* (scheme w/o completion)
#-

#include `ROOT'/def/BT5.def

#procedure zGT3
* vanishing sub-topologies
if ((count(d7,1) >= 0)) 
  discard;
if ((count(d5,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT3(MM)
* no partial fraction decomposition
#endprocedure

#procedure mGT3tBT5(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,6,7) to (1,2,3,4,5,6,7)
id (`marker') = BT5;
mu replace_(d7,d7, d6,d6, d5,d5, d4,d4, d3,d3, d2,d2, d1,d1);
mu replace_(v1,v1, v2,v2);
.sort
#call cBT5((`MM'))
.sort
#call gBT5((`MM'),(`M2ep'))
.sort
#call zBT5
.sort
#endprocedure

#procedure GT3
#message this is "GT3"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT3
#call pGT3(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT3tBT5(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT3\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT5'
#message symmetrize "BT5"...
#call yBT5
.sort
#endif
* load reduction tables
#ifdef `useBT5'
#message read table for "BT5"...
#include `ROOT'/tab/BT5.inx
.sort
#endif
#endprocedure

*--#] GT3 :



*--#[ GT4 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:56:29.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d6 (hb), -p1 - v1 + v2,     5 -- 4
*   2: d7 (gl), p1 - p2 + v1 - v2, 6 -- 4
*   3: d5 (gl), -p1 + v2,          6 -- 3
*   4: d4 (gl), -v2,               5 -- 3
*   5: d2 (gl), p1 + v1,           5 -- 1
*   6: d1 (gl), -v1,               2 -- 1
*   7: d3 (gl), p2 - v1,           6 -- 2
* (scheme w/o completion)
#-

#include `ROOT'/def/BT6.def

#procedure zGT4
* vanishing sub-topologies
if ((count(d6,1) >= 0)) 
  discard;
if ((count(d4,1) >= 0)) 
  discard;
if ((count(d3,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT4(MM)
* no partial fraction decomposition
#endprocedure

#procedure mGT4tBT6(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,6,7) to (1,2,3,4,5,6,7)
id (`marker') = BT6;
mu replace_(d6,d6, d7,d7, d5,d5, d4,d4, d2,d2, d1,d1, d3,d3);
mu replace_(v1,v1, v2,v2);
.sort
#call cBT6((`MM'))
.sort
#call gBT6((`MM'),(`M2ep'))
.sort
#call zBT6
.sort
#endprocedure

#procedure GT4
#message this is "GT4"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT4
#call pGT4(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT4tBT6(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT4\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT6'
#message symmetrize "BT6"...
#call yBT6
.sort
#endif
* load reduction tables
#ifdef `useBT6'
#message read table for "BT6"...
#include `ROOT'/tab/BT6.inx
.sort
#endif
#endprocedure

*--#] GT4 :



*--#[ GT5 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:56:31.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d7 (gl), p1 - p2 + v1 - v2, 6 -- 4
*   2: d6 (gl), -p1 - v1 + v2,     5 -- 4
*   3: d5 (gl), -p1 + v2,          6 -- 3
*   4: d4 (gl), -v2,               5 -- 3
*   5: d3 (hb), p2 - v1,           6 -- 2
*   6: d1 (gl), -v1,               2 -- 1
*   7: d2 (gl), p1 + v1,           5 -- 1
* (scheme w/o completion)
#-

#include `ROOT'/def/BT7.def

#procedure zGT5
* vanishing sub-topologies
if ((count(d3,1) >= 0)) 
  discard;
if ((count(d6,1) >= 0) &&
    (count(d7,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0) &&
    (count(d6,1) >= 0)) 
  discard;
if ((count(d4,1) >= 0) &&
    (count(d5,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0) &&
    (count(d4,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT5(MM)
* no partial fraction decomposition
#endprocedure

#procedure mGT5tBT7(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,6,7) to (1,2,3,4,5,6,7)
id (`marker') = BT7;
mu replace_(d7,d7, d6,d6, d5,d5, d4,d4, d3,d3, d1,d1, d2,d2);
mu replace_(v1,v1, v2,v2);
.sort
#call cBT7((`MM'))
.sort
#call gBT7((`MM'),(`M2ep'))
.sort
#call zBT7
.sort
#endprocedure

#procedure GT5
#message this is "GT5"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT5
#call pGT5(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT5tBT7(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT5\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT7'
#message symmetrize "BT7"...
#call yBT7
.sort
#endif
* load reduction tables
#ifdef `useBT7'
#message read table for "BT7"...
#include `ROOT'/tab/BT7.inx
.sort
#endif
#endprocedure

*--#] GT5 :



*--#[ GT6 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:56:34.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d7 (hb), v1 + v2,  6 -- 5
*   2: d1 (gl), -v1,      2 -- 1
*   3: d2 (gl), -p1 + v1, 5 -- 1
*   4: d3 (gl), -p2 - v1, 6 -- 2
*   5: d4 (gl), -v2,      4 -- 3
*   6: d6 (gl), p2 - v2,  6 -- 4
*   7: d5 (gl), p1 + v2,  5 -- 3
* (scheme w/o completion)
#-

#include `ROOT'/def/BT8.def

#procedure zGT6
* vanishing sub-topologies
if ((count(d7,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0) &&
    (count(d3,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0) &&
    (count(d5,1) >= 0)) 
  discard;
if ((count(d3,1) >= 0) &&
    (count(d6,1) >= 0)) 
  discard;
if ((count(d5,1) >= 0) &&
    (count(d6,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT6(MM)
* no partial fraction decomposition
#endprocedure

#procedure mGT6tBT8(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,6,7) to (1,2,3,4,5,6,7)
id (`marker') = BT8;
mu replace_(d7,d7, d1,d1, d2,d2, d3,d3, d4,d4, d6,d6, d5,d5);
mu replace_(v1,v1, v2,v2);
.sort
#call cBT8((`MM'))
.sort
#call gBT8((`MM'),(`M2ep'))
.sort
#call zBT8
.sort
#endprocedure

#procedure GT6
#message this is "GT6"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT6
#call pGT6(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT6tBT8(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT6\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT8'
#message symmetrize "BT8"...
#call yBT8
.sort
#endif
* load reduction tables
#ifdef `useBT8'
#message read table for "BT8"...
#include `ROOT'/tab/BT8.inx
.sort
#endif
#endprocedure

*--#] GT6 :



*--#[ GT7 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:56:37.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d7 (gl), v1 + v2,  6 -- 5
*   2: d6 (hb), p2 - v2,  6 -- 4
*   3: d4 (gl), -v2,      4 -- 3
*   4: d5 (gl), p1 + v2,  5 -- 3
*   5: d3 (gl), -p2 - v1, 6 -- 2
*   6: d1 (gl), -v1,      2 -- 1
*   7: d2 (gl), -p1 + v1, 5 -- 1
* (scheme w/o completion)
#-

#include `ROOT'/def/BT9.def

#procedure zGT7
* vanishing sub-topologies
if ((count(d6,1) >= 0)) 
  discard;
if ((count(d5,1) >= 0) &&
    (count(d7,1) >= 0)) 
  discard;
if ((count(d3,1) >= 0) &&
    (count(d7,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0) &&
    (count(d7,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0) &&
    (count(d5,1) >= 0)) 
  discard;
if ((count(d1,1) >= 0) &&
    (count(d2,1) >= 0) &&
    (count(d3,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT7(MM)
* no partial fraction decomposition
#endprocedure

#procedure mGT7tBT9(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,6,7) to (1,2,3,4,5,6,7)
id (`marker') = BT9;
mu replace_(d7,d7, d6,d6, d4,d4, d5,d5, d3,d3, d1,d1, d2,d2);
mu replace_(v1,v1, v2,v2);
.sort
#call cBT9((`MM'))
.sort
#call gBT9((`MM'),(`M2ep'))
.sort
#call zBT9
.sort
#endprocedure

#procedure GT7
#message this is "GT7"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT7
#call pGT7(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT7tBT9(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT7\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT9'
#message symmetrize "BT9"...
#call yBT9
.sort
#endif
* load reduction tables
#ifdef `useBT9'
#message read table for "BT9"...
#include `ROOT'/tab/BT9.inx
.sort
#endif
#endprocedure

*--#] GT7 :



*--#[ GT8 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:56:40.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d7 (hb), -p1 + p2 + v1 + v2, 6 -- 5
*   2: d2 (gl), -p1 + v1,           5 -- 1
*   3: d3 (gl), -p2 - v1,           6 -- 2
*   4: d1 (gl), -v1,                2 -- 1
*   5: d6 (gl), p1 - v2,            6 -- 4
*   6: d5 (gl), p2 + v2,            5 -- 3
*   7: d4 (gl), -v2,                4 -- 3
* (scheme w/o completion)
#-

#include `ROOT'/def/BT10.def

#procedure zGT8
* vanishing sub-topologies
if ((count(d7,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0) &&
    (count(d3,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0) &&
    (count(d5,1) >= 0)) 
  discard;
if ((count(d3,1) >= 0) &&
    (count(d6,1) >= 0)) 
  discard;
if ((count(d5,1) >= 0) &&
    (count(d6,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT8(MM)
* no partial fraction decomposition
#endprocedure

#procedure mGT8tBT10(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,6,7) to (1,2,3,4,5,6,7)
id (`marker') = BT10;
mu replace_(d7,d7, d2,d2, d3,d3, d1,d1, d6,d6, d5,d5, d4,d4);
mu replace_(v1,v1, v2,v2);
.sort
#call cBT10((`MM'))
.sort
#call gBT10((`MM'),(`M2ep'))
.sort
#call zBT10
.sort
#endprocedure

#procedure GT8
#message this is "GT8"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT8
#call pGT8(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT8tBT10(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT8\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT10'
#message symmetrize "BT10"...
#call yBT10
.sort
#endif
* load reduction tables
#ifdef `useBT10'
#message read table for "BT10"...
#include `ROOT'/tab/BT10.inx
.sort
#endif
#endprocedure

*--#] GT8 :



*--#[ GT9 :

*  
* Generated by TopoID-2.0 (2016-XX-XX) on 2016-06-09 17:56:43.
*  
*   Note: Delete this comment on modification.
*  

#+
* topology line definitions:
*   1: d7 (gl), -p1 + p2 + v1 + v2, 6 -- 5
*   2: d6 (hb), p1 - v2,            6 -- 4
*   3: d4 (gl), -v2,                4 -- 3
*   4: d5 (gl), p2 + v2,            5 -- 3
*   5: d3 (gl), -p2 - v1,           6 -- 2
*   6: d1 (gl), -v1,                2 -- 1
*   7: d2 (gl), -p1 + v1,           5 -- 1
* (scheme w/o completion)
#-

#include `ROOT'/def/BT11.def

#procedure zGT9
* vanishing sub-topologies
if ((count(d6,1) >= 0)) 
  discard;
if ((count(d5,1) >= 0) &&
    (count(d7,1) >= 0)) 
  discard;
if ((count(d3,1) >= 0) &&
    (count(d7,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0) &&
    (count(d7,1) >= 0)) 
  discard;
if ((count(d2,1) >= 0) &&
    (count(d5,1) >= 0)) 
  discard;
if ((count(d1,1) >= 0) &&
    (count(d2,1) >= 0) &&
    (count(d3,1) >= 0)) 
  discard;
#endprocedure

#procedure pGT9(MM)
* no partial fraction decomposition
#endprocedure

#procedure mGT9tBT11(marker,MM,M2ep)
* subset 1
* map from (1,2,3,4,5,6,7) to (1,2,3,4,5,6,7)
id (`marker') = BT11;
mu replace_(d7,d7, d6,d6, d4,d4, d5,d5, d3,d3, d1,d1, d2,d2);
mu replace_(v1,v1, v2,v2);
.sort
#call cBT11((`MM'))
.sort
#call gBT11((`MM'),(`M2ep'))
.sort
#call zBT11
.sort
#endprocedure

#procedure GT9
#message this is "GT9"...
* apply simplifications
#message apply simplifications...
repeat;
#call zGT9
#call pGT9(s)
endrepeat;
.sort
* map to independents
#message map to independents...
mu marker;
#call mGT9tBT11(marker,s,[(mu^2/s)^ep])
.sort
if (count(marker,1) != 0) 
  exit "Error: \"GT9\" not completely mapped to independents.";
* symmetrize
#ifdef `symBT11'
#message symmetrize "BT11"...
#call yBT11
.sort
#endif
* load reduction tables
#ifdef `useBT11'
#message read table for "BT11"...
#include `ROOT'/tab/BT11.inx
.sort
#endif
#endprocedure

*--#] GT9 :

